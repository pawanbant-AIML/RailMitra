# Core API
fastapi==0.110.2
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.31
psycopg2-binary==2.9.9

# Data validation
pydantic==2.7.4
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Logging
loguru==0.7.2

# HTTP client (used for HuggingFace API calls)
requests==2.31.0
httpx==0.27.0

# LangChain agent framework
langchain==0.2.16
langchain-core==0.2.38
langchain-community==0.2.16

# Auth
pyjwt==2.8.0

# NLP – lightweight fuzzy matching (kept for station resolution)
# spacy is removed to reduce Render memory footprint.
# Station resolution now uses difflib (stdlib) in station_repo.py